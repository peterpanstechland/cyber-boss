# HEARTBEAT.md — 铁哥定期巡检

## 巡检任务（每次心跳轮询执行 1-2 项）

### 🔴 任务池健康检查
读取 Bitable 任务池，检查：
- 有没有过期的 P0/P1（截止时间 < 今天 且 状态 ≠ 已完成）
- 连续拖延天数 ≥ 3 的任务
- 如果有，给老板发消息提醒

```bash
node /home/node/.openclaw/skills/feishu-bitable/feishu-bitable.mjs list --base <YOUR_BITABLE_BASE_ID> --table <YOUR_BITABLE_TABLE_ID>
```

### 📋 今日任务状态
如果已经做过晨间排程，检查飞书任务完成状态：
- 用 `feishu-task list` 查看今天的任务
- 如果快到晚 9 点且有未完成任务，提前催一下

### 📝 Memory 维护
- 检查 `memory/daily-log.md` 是否已创建今天的条目
- 如果有待处理的提醒（之前写在 memory 里的），执行它
- 定期（每 3-5 天）整理 memory 目录，把重要信息提炼到 MEMORY.md

### ⏰ 定时提醒检查
- 检查 `at -l` 看有没有待执行的定时任务
- 检查 crontab 是否正常

## 巡检节奏

| 时间段 | 行为 |
|--------|------|
| 08:00-09:00 | 准备晨间排程数据，等 cron 触发 |
| 09:00-12:00 | 检查任务录入、回应老板消息 |
| 12:00-14:00 | 安静，除非有 P0 |
| 14:00-18:00 | 中途检查进度，处理紧急插入 |
| 18:00-21:00 | 准备晚间复盘数据 |
| 21:00-23:00 | 晚间复盘，写决策日志 |
| 23:00-08:00 | 静默，不发消息 |

## HEARTBEAT_OK 条件

以下情况直接返回 HEARTBEAT_OK，不做任何操作：
- 23:00-08:00（深夜静默）
- 距上次主动消息 < 30 分钟
- 没有过期 P0，没有拖延告警，没有待执行提醒
